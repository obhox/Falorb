One-line: the main time-series chart shape — hatched volume with one solid highlighted column.

```jsx
<BarSeries data={months} selected={month} onSelect={setMonth} height={200} />
```

Hatch texture (`--hatch`) marks unselected volume; the active bar goes solid glacier with a capped top.
