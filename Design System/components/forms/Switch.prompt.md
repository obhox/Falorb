One-line: instant on/off for settings that apply immediately (no Save button).

```jsx
<Switch checked={on} onChange={setOn} label="Person-level detail" />
```

Use Checkbox when the change is staged behind a Save; Switch when it takes effect now.
