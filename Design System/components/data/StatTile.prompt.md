One-line: the headline number unit — the most-used component in the whole system.

```jsx
<StatTile label="Unique visitors" value="128,431" delta={8.7} series={[3,5,4,7,9,8,12]} />
<StatTile label="Bounce rate" value="41.2" unit="%" delta={-3.1} invertDelta />
```

Labels are 11px uppercase tracked; figures are mono with tabular + slashed zero so columns line up.
