One-line: small non-interactive status/count chip; square-ish (6px radius), never a pill.

```jsx
<Badge tone="up" mono>+8.7%</Badge>
<Badge tone="neutral" dot>Live</Badge>
```

Set `mono` for anything numeric. Reserve up/down for change values so colour keeps meaning.
