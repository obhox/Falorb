One-line: trend shape without axes — inside StatTiles, table rows, and property cards.

```jsx
<Sparkline data={[4,6,5,9,8,12,11,15]} height={28} />
```

One series only. Two or more series means a real chart, not a sparkline.
