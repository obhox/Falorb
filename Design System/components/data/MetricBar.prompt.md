One-line: ranked breakdown rows (top pages, referrers, countries) where the row itself is the bar.

```jsx
<MetricBar label="/pricing" value="4,182" share={64} meta="12.1%" onClick={drill} />
```

Fill is alpha-white; hover tints it glacier. Sort descending and cap at 5–8 rows with a "Show all".
