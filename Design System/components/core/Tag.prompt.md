One-line: pill used for applied filters and segment membership — interactive counterpart to Badge.

```jsx
<Tag icon={<Icon name="filter" size={12} />} onRemove={() => {}}>country = DE</Tag>
```

Pill radius distinguishes Tag (a filter you can drop) from Badge (a fact you can't).
