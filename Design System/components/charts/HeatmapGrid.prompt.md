One-line: when-does-it-happen grid — weekday × hour activity, deploy windows, error density.

```jsx
<HeatmapGrid rows={["Mon","Tue","Wed"]} cols={hours} values={matrix} />
```

Intensity is glacier alpha from 10% to 88%, so it never introduces a second hue. Includes its own scale strip.
