One-line: single-line text entry; the inset well distinguishes "you type here" from every other surface.

```jsx
<Input label="Site domain" mono placeholder="app.example.com" iconLeft={<Icon name="globe" size={14} />} />
```

Focus = glacier border + 3px soft ring. `invalid` swaps to the down-signal red and colours the hint.
