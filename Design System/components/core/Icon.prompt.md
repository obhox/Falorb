One-line: renders a Lucide glyph at Falorb's 1.5px stroke weight; the only sanctioned icon source.

```jsx
<Icon name="globe" size={16} />
```

Never swap in emoji or a hand-drawn SVG. Icons are `currentColor` so they inherit the control's text colour.
