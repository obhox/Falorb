One-line: the container for every block of data on a Falorb screen.

```jsx
<Card title="Expense analysis" subtitle="Last 30 days" action={<Select value="Transactions" options={["Transactions","Sessions"]} />}>
  …
</Card>
```

Hairline border + soft dark shadow + inset top highlight. No coloured left borders, no gradients.
