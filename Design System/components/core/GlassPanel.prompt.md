One-line: frosted overlay surface for anything floating above data — chart tooltips, popovers, command palette, sticky toolbars.

```jsx
<GlassPanel padding={12} radius="var(--radius-card)">…</GlassPanel>
```

Always the full recipe: alpha-white fill + hairline border + blur. Blur alone reads muddy on graphite.
