One-line: multi-select control for filters and settings lists.

```jsx
<Checkbox checked={on} onChange={setOn} label="Respect Do Not Track" description="Drops the event before it reaches the queue." />
```

4px radius box, glacier fill when checked, graphite check glyph.
