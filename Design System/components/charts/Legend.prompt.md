One-line: series key for any multi-series chart; 11px, dots for areas/bars, short rules for lines.

```jsx
<Legend items={[{label:"Visitors",shape:"line"},{label:"Sessions",shape:"line",color:"var(--series-2)"}]} />
```

Drop the legend entirely for single-series charts — the title already says what it is.
