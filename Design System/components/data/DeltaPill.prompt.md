One-line: period-over-period change; the only sanctioned use of green/red.

```jsx
<DeltaPill value={8.7} />
<DeltaPill value={-6.3} invert />
```

Never use signal colours for anything that isn't a delta or status.
