One-line: value picker for ranges, granularities, and property scoping.

```jsx
<Select variant="bare" value="All properties" options={["All properties","falorb.io","docs"]} onChange={set} />
```

Use `bare` in page headers where the label reads as prose; `control` inside cards and forms.
